apiVersion: apps/v1
kind: Deployment
metadata:
  name: quick-admin-api
  labels:
    app: quick-admin-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: quick-admin-api
  template:
    metadata:
      labels:
        app: quick-admin-api
    spec:
      terminationGracePeriodSeconds: 40
      containers:
        - name: api
          image: ghcr.io/your-org/quick-admin-api:0.1.0
          ports:
            - containerPort: 9009
              name: http
          env:
            - name: LIGHTNING_APP_ENV
              value: prod
            - name: LIGHTNING_SERVER_PORT
              value: "9009"
            - name: LIGHTNING_DATABASE_DSN
              valueFrom:
                secretKeyRef:
                  name: quick-admin-secret
                  key: database-dsn
            - name: LIGHTNING_REDIS_ADDR
              valueFrom:
                configMapKeyRef:
                  name: quick-admin-config
                  key: redis-addr
            - name: LIGHTNING_REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: quick-admin-secret
                  key: redis-password
            - name: LIGHTNING_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: quick-admin-secret
                  key: jwt-secret
          resources:
            requests:
              memory: 256Mi
              cpu: 250m
            limits:
              memory: 1Gi
              cpu: "1"
          startupProbe:
            httpGet:
              path: /health/startup
              port: http
            periodSeconds: 5
            failureThreshold: 30
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: quick-admin-scheduler
  labels:
    app: quick-admin-scheduler
spec:
  replicas: 1
  selector:
    matchLabels:
      app: quick-admin-scheduler
  template:
    metadata:
      labels:
        app: quick-admin-scheduler
    spec:
      terminationGracePeriodSeconds: 40
      containers:
        - name: scheduler
          image: ghcr.io/your-org/quick-admin-scheduler:0.1.0
          env:
            - name: LIGHTNING_APP_ENV
              value: prod
            - name: LIGHTNING_DATABASE_DSN
              valueFrom:
                secretKeyRef:
                  name: quick-admin-secret
                  key: database-dsn
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 512Mi
              cpu: 500m
