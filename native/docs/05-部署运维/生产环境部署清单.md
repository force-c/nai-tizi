# 生产环境部署清单

- [ ] 镜像已通过 `go test ./...`、`go vet ./...`、`go test -race ./...`、Swagger freshness 和 Docker build。
- [ ] 使用不可变镜像版本或 digest，没有使用 `latest`。
- [ ] `QUICK_ADMIN_APP_ENV=prod`。
- [ ] PostgreSQL DSN、Redis 密码、JWT 和第三方密钥由 Secret 注入，没有写入镜像或 ConfigMap。
- [ ] JWT 密钥至少 32 字符并有轮换流程。
- [ ] PostgreSQL 已配置备份、恢复演练和最小权限账号。
- [ ] Goose migration 已在预发布空库和现有库验证。
- [ ] API 只监听内部网络，通过 Nginx/Ingress 同源代理。
- [ ] 生产 CORS 关闭。
- [ ] `/health/startup`、`/health/live`、`/health/ready` 探针已配置为端口 `9009`。
- [ ] 日志采集系统已确认会脱敏密码、Token、验证码和第三方密钥。
- [ ] 已通过 `cmd/usermgr` 创建首个管理员，密码未出现在命令行、日志或镜像中。
- [ ] 默认示例 Secret 已替换，没有部署 `k8s/secret.yaml.example`。
